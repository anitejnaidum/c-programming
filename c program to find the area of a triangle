#include <stdio.h>
int main()
{
    float base,height,area;
    printf("enter the base and height of the triangle: ");
    scanf("%f%f",&base,&height);
    area=(base*height)/2;
    printf("%f is the area if the triangle",area);
    return 0;
}
